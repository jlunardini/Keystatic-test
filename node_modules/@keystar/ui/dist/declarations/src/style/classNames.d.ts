import { ClassNamesArg } from '@emotion/css';
export declare const classNamePrefix = "ksv";
export declare const resetClassName: string;
export declare function voussoirClassName(className: string): string;
/**
 * A thin wrapper around [Emotion's `cx`
 * function](https://emotion.sh/docs/@emotion/css#cx) that includes the reset
 * class name.
 */
export declare function classNames(...inputs: ClassNamesArg[]): string;
/**
 * A thin wrapper around Map to manage component class names. Keys are prefixed
 * with the component name and Voussoir's prefix.
 */
export declare class ClassList extends Map {
    prefix: string;
    constructor(componentName: string);
    root(): string;
    declare(element: string): any;
    selector(element: string): string;
    childSelector(element: string): string;
}
