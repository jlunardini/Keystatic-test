import { i as integer, c as component } from '../../dist/api-958e5fc6.react-server.esm.js';
import { M as text } from '../../dist/index-f75136a3.react-server.esm.js';
import '../../dist/empty-field-ui-1936cae8.react-server.esm.js';
import 'react/jsx-runtime';
import 'emery';
import '@sindresorhus/slugify';
import '@braintree/sanitize-url';
import '@markdoc/markdoc';
import 'slate';
import 'emery/assertions';
import 'js-base64';
import '@emotion/weak-memoize';

function CloudImagePreview() {}

const cloudImageSchema = {
  src: text({
    label: 'URL',
    validation: {
      length: {
        min: 1
      }
    }
  }),
  alt: text({
    label: 'Alt text'
  }),
  height: integer({
    label: 'Height'
  }),
  width: integer({
    label: 'Width'
  })
};

/** @deprecated Experimental */
function cloudImage(args) {
  return component({
    label: args.label,
    schema: cloudImageSchema,
    preview: CloudImagePreview,
    chromeless: true,
    toolbar: null
  });
}

export { cloudImage };
